# Flask
FLASK_SECRET_KEY=change_me_for_prod
PORT=8000
SESSION_COOKIE_SECURE=False

# MercadoLibre OAuth
MELI_CLIENT_ID=your_client_id
MELI_CLIENT_SECRET=your_client_secret
MELI_REDIRECT_URI=http://localhost:8000/callback
# If you need a different site auth page (MLM/MLB), override:
# MELI_AUTHORIZE_URL=https://auth.mercadolibre.com.mx/authorization

# Token endpoint rarely changes; override only if necessary:
# MELI_TOKEN_URL=https://api.mercadolibre.com/oauth/token

# Database (pick one)
# SQLite (dev)
DATABASE_URL=sqlite:///tokens.db
# Postgres
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname
# MySQL
# DATABASE_URL=mysql+pymysql://user:pass@host:3306/dbname
# SQL Server (ODBC)
# DATABASE_URL="mssql+pyodbc:///?odbc_connect=DRIVER%3D%7BODBC+Driver+17+for+SQL+Server%7D;SERVER%3Dhost;DATABASE%3Ddb;UID%3Duser;PWD%3Dpass"
